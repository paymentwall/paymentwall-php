default:
  suites:
    default:
      paths:
        - '%paths.base%/features'
      contexts:
        - FeatureContext
        - WidgetContext
        - PingbackContext
        - ChargeContext
